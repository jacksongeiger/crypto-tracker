# TODO

- [ ] Migration `downgrade()` for `70345c3ebb63` doesn't drop the `source_type` ENUM — add `op.execute("DROP TYPE source_type")` so downgrade→upgrade cycles don't fail with "type already exists".
- [ ] `Source.updated_at` uses SQLAlchemy `onupdate=func.now()` which only fires on ORM updates; replace with a Postgres trigger if raw SQL `UPDATE`s need to bump it too.
- [ ] `~/.openclaw/openclaw.json` fails `openclaw config validate` because `channels.telegram.dmPolicy` and `channels.telegram.groupPolicy` are required by the current schema but absent — blocks `openclaw config patch`; use `config set` until fixed.
- [ ] Migrate synthesis skill from `google-generativeai` (end-of-life per Google) to the new `google-genai` SDK. Currently a `FutureWarning` is suppressed in `synthesize.py`.
- [ ] Synthesis v4 still produces *within-company* multi-event themes (e.g. Circle Arc presale + Circle AI agent launch under one theme). The primary_signal_id schema defeated *cross-entity* bucketing but not *same-entity* bucketing. v5 candidate fix: tighten "source_signal_ids must all be about the same event" — possibly via an LLM self-check pass that re-reads each theme's sources and drops ones not about the primary event.
- [x] ~~Register a free FRED API key~~ — done; macro skill now ingests 9/10 indicators successfully.
- [ ] Macro skill's gold series `GOLDAMGBD228NLBM` (and the PM variant `GOLDPMGBD228NLBM`) both return 400 from FRED — the LBMA gold fix series have been discontinued. Find a replacement series ID and update `INDICATORS` in `skills/macro/scripts/ingest.py`. The other 9 indicators ingest cleanly.
- [x] ~~Run a real persisted v7 synthesis once Gemini Flash daily quota resets~~ — done; brief `71b3f386-1f92-4cc8-b1cd-02c517b2e7ba` persisted 2026-05-13.
- [ ] v7 same-event constraint fixed within-entity bucketing reliably but did NOT robustly fix cross-entity within-type bucketing. Two production runs both show themes joining different subjects with "and" (DTCC + Franklin Templeton; DEX volumes + chain fees). The Exp 1 dry-run that won the A/B was likely sampling-favorable. The Exp 4 two-pass approach (skipped last night because Exp 1 passed) is the structural candidate fix. Trigger v8 work when: (a) bucketing pattern persists across 3+ production runs, AND (b) other higher-leverage work (heartbeats, history page, deployment) is shipped.

## Deployment (Oracle ARM, 192.18.128.170)

- [x] ~~**HTTPS for the dashboard** — Caddy reverse-proxy on the ARM server~~ — Caddy 2.11.3 under systemd at https://192-18-128-170.nip.io/ reverse-proxying to localhost:3000. Real Let's Encrypt cert (issuer `Let's Encrypt E7`) acquired via tls-alpn-01 challenge, valid 2026-05-13 → 2026-08-11. HTTP/2 + TLS 1.3. All routes externally 200. Iptables @reboot entries persist 80/443.
- [x] ~~**Open Oracle Cloud Security List ingress for 80/tcp and 443/tcp**~~ — done by user 2026-05-13.
- [ ] **DuckDNS not used** — currently on `nip.io` (no-registration). If you ever want a memorable hostname (e.g., `crypto-tracker-jg.duckdns.org`), set up a DuckDNS account, paste token onto server, and update `/etc/caddy/Caddyfile` site name + reload. nip.io has no expiry/renewal cycle, so this is purely cosmetic.
- [ ] **Verify Caddy auto-renews Let's Encrypt** — Caddy renews automatically at the ~60-day mark. Spot-check by 2026-07-15 with `echo | openssl s_client -servername 192-18-128-170.nip.io -connect 192-18-128-170.nip.io:443 2>/dev/null | openssl x509 -noout -enddate` (should be > 30 days out).
- [ ] **OpenClaw cron, not system cron** — pivoted to system cron during Phase 1 because the single-device server has only `operator.read + operator.pairing` scopes; `openclaw devices approve` requires a *second* device with `operator.approvals` to grant the upgrade. Re-evaluate when (a) the OpenClaw scope-bootstrap flow improves for single-device hosts, or (b) we pair a second admin device. Current state: 6 jobs in system crontab via `scripts/install_system_cron.sh`, logged through `scripts/run_cron_job.sh`, monitored by `scripts/verify_pipeline.py`.
- [ ] **Disable laptop cron** — leave the laptop's `openclaw cron` entries enabled for now as a backup; flip OFF after the server runs 24h green (one full pipeline cycle observed via `verify_pipeline.py`). Disable command on the laptop: `openclaw cron remove <id>` for each of the 6 entries, or remove the marker block from the laptop's crontab if running there.
- [ ] **Telegram delivery** — bot token is on the server in `backend/.env` but no skill consumes it yet. Wire `synthesize-brief` to push the formatted brief to a Telegram chat after persistence. Triggers HTTPS work above.
- [ ] **Backups** — no pg_dump cron on the server yet. Add a daily `pg_dump crypto_tracker | gzip` to `/home/ubuntu/backups/` with 14-day rotation once the brief becomes load-bearing.
- [ ] **Ingestion idempotency for same-day re-runs** — `macro` and `predictions` (and likely the others) crash with `UniqueViolation` on `uq_raw_signals_url` when re-triggered the same day with no new entries. The scheduled 06:30/06:40 PT runs work because data is fresh, so this is purely a manual-rerun safety bug. Fix: wrap inserts in `ON CONFLICT (url) DO NOTHING` (or per-row try/except). Discovered during 2026-05-13 ARM deployment smoke test.
